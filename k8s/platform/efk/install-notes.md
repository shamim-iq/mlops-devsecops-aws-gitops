# EFK Install Notes

Install EFK only after the application and rollout path are working. Keep the demo footprint small: 1-day retention, one small Elasticsearch data volume, and no external load balancer for Kibana unless evidence capture requires it.
